package com.attendance.api.jwt;

/*import java.util.Date;

import com.attendance.api.entity.LoginForm;

import io.jsonwebtoken.Claims;
import io.jsonwebtoken.Jwts;
import io.jsonwebtoken.SignatureAlgorithm;
*/
public class JWTUtils {
	/*
	private static String secret = "This_is_secret";
	private static long expiryDuration = 60 * 60;


	 * public String generateJwt(LoginForm login) {
	 * 
	 * long milliTime = System.currentTimeMillis(); long expiryTime = milliTime +
	 * expiryDuration * 1000;
	 * 
	 * Date issuedAt = new Date(); Date expiryAt = new Date(expiryTime);
	 * 
	 * Claims claims =
	 * Jwts.claims().setIssuer(login.getEmail().toString()).setIssuedAt(issuedAt)
	 * .setExpiration(expiryAt);
	 * 
	 * return Jwts.builder().setClaims(claims).signWith(SignatureAlgorithm.HS512,
	 * secret).compact();
	 * 
	 * }
	 */
}
